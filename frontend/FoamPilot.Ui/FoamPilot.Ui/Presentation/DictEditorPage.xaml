<Page x:Class="FoamPilot.Ui.Presentation.DictEditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Background="{ThemeResource BackgroundBrush}">

  <Grid Padding="24" ColumnSpacing="16">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="280" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <!-- Left: case + file tree -->
    <Grid RowSpacing="12">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <TextBlock Text="Dict Editor"
          Style="{ThemeResource TitleLargeTextBlockStyle}" />

      <ComboBox x:Name="CaseCombo" Grid.Row="1" Header="Case"
          HorizontalAlignment="Stretch"
          SelectionChanged="CaseCombo_SelectionChanged" />

      <muxc:TreeView x:Name="FileTree" Grid.Row="2"
          ItemInvoked="FileTree_ItemInvoked" />
    </Grid>

    <!-- Right: file editor -->
    <Grid Grid.Column="1" RowSpacing="8">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <StackPanel Orientation="Horizontal" Spacing="8">
        <TextBlock x:Name="FilePathText" Text="No file selected"
            Style="{ThemeResource BodyStrongTextBlockStyle}"
            VerticalAlignment="Center" />
        <Button Content="Save" Click="Save_Click" Padding="8,4" />
        <Button Content="Revert" Click="Revert_Click" Padding="8,4" />
        <TextBlock x:Name="EditorStatus"
            Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.7"
            VerticalAlignment="Center" Margin="8,0,0,0" />
      </StackPanel>

      <TextBox x:Name="Editor" Grid.Row="1"
          AcceptsReturn="True"
          TextWrapping="NoWrap"
          FontFamily="Cascadia Code, Consolas, Courier New, monospace"
          FontSize="13"
          ScrollViewer.VerticalScrollBarVisibility="Auto"
          ScrollViewer.HorizontalScrollBarVisibility="Auto" />
    </Grid>
  </Grid>
</Page>
