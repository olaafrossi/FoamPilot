<Page x:Class="FoamPilot.Ui.Presentation.QuickRunPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource BackgroundBrush}">

  <Grid Padding="24" RowSpacing="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Text="Quick Run — Cavity Tutorial"
        Style="{ThemeResource TitleLargeTextBlockStyle}" />

    <!-- Status + controls -->
    <StackPanel Grid.Row="1" Spacing="8">
      <TextBlock x:Name="StatusText" Text="Ready. Click a button to begin."
          Style="{ThemeResource BodyLargeTextBlockStyle}" Opacity="0.8" />
      <ProgressBar x:Name="Progress" IsIndeterminate="False" Visibility="Collapsed" />
    </StackPanel>

    <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
      <Button x:Name="BtnCreateCase" Content="1. Create Case" Click="CreateCase_Click" Padding="12,8" />
      <Button x:Name="BtnMesh" Content="2. Run blockMesh" Click="RunMesh_Click" Padding="12,8" IsEnabled="False" />
      <Button x:Name="BtnSolve" Content="3. Run icoFoam" Click="RunSolver_Click" Padding="12,8" IsEnabled="False" />
      <Button x:Name="BtnCheckResults" Content="4. Check Results" Click="CheckResults_Click" Padding="12,8" IsEnabled="False" />
      <AppBarSeparator />
      <Button x:Name="BtnParaView" Content="Open in ParaView" Click="OpenParaView_Click" Padding="12,8" IsEnabled="False" />
      <Button x:Name="BtnOpenFolder" Content="Open Case Folder" Click="OpenFolder_Click" Padding="12,8" IsEnabled="False" />
    </StackPanel>

    <!-- Log output -->
    <Border Grid.Row="3"
        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
        BorderThickness="1" CornerRadius="8" Padding="8">
      <ScrollViewer x:Name="LogScroll">
        <TextBlock x:Name="LogOutput"
            FontFamily="Cascadia Code, Consolas, Courier New, monospace"
            FontSize="12"
            TextWrapping="Wrap"
            IsTextSelectionEnabled="True" />
      </ScrollViewer>
    </Border>
  </Grid>
</Page>
