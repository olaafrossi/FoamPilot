<Page x:Class="FoamPilot.Ui.Presentation.MySimulationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource BackgroundBrush}">

  <Grid Padding="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Text="My Simulations"
        FontSize="24" FontWeight="SemiBold"
        Margin="0,0,0,8" />

    <TextBlock Grid.Row="1"
        Text="Your previous simulation cases. Click to open the case folder or launch in ParaView."
        Foreground="Gray" TextWrapping="Wrap"
        Margin="0,0,0,16" />

    <ListView x:Name="CaseList" Grid.Row="2"
        SelectionMode="None" IsItemClickEnabled="True"
        ItemClick="CaseList_ItemClick">
      <ListView.ItemTemplate>
        <DataTemplate>
          <Grid Padding="8,12" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="160" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel>
              <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" />
              <TextBlock Text="{Binding Path}" FontSize="11" Foreground="Gray" />
            </StackPanel>

            <TextBlock Grid.Column="1" Text="{Binding Modified}"
                FontSize="11" Foreground="Gray"
                VerticalAlignment="Center" />

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
              <Button Content="Open Folder" Tag="{Binding Name}" Click="OpenFolder_Click" />
              <Button Content="ParaView" Tag="{Binding Name}" Click="ParaView_Click" />
              <Button Content="Delete" Tag="{Binding Name}" Click="Delete_Click"
                  Foreground="OrangeRed" />
            </StackPanel>
          </Grid>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
</Page>
