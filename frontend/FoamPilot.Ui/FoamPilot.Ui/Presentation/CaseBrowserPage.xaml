<Page x:Class="FoamPilot.Ui.Presentation.CaseBrowserPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource BackgroundBrush}">

  <Grid Padding="24" RowSpacing="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Text="Case Browser"
        Style="{ThemeResource TitleLargeTextBlockStyle}" />

    <!-- Create case -->
    <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8">
      <ComboBox x:Name="TemplateCombo" Header="Template" Width="200" />
      <TextBox x:Name="NewCaseNameBox" Header="Case Name" Width="200"
          PlaceholderText="my_case" />
      <Button Content="Create Case" Click="CreateCase_Click"
          VerticalAlignment="Bottom" Padding="12,8" />
      <Button Content="Refresh" Click="Refresh_Click"
          VerticalAlignment="Bottom" Padding="12,8" />
      <TextBlock x:Name="StatusText" VerticalAlignment="Bottom"
          Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.7"
          Margin="8,0,0,6" />
    </StackPanel>

    <!-- Cases list -->
    <ListView x:Name="CasesList" Grid.Row="2" SelectionMode="Single">
      <ListView.ItemTemplate>
        <DataTemplate>
          <Grid Padding="12,8" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="2*" />
              <ColumnDefinition Width="3*" />
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding Name}" VerticalAlignment="Center"
                Style="{ThemeResource BodyStrongTextBlockStyle}" />

            <TextBlock Text="{Binding Modified}" Grid.Column="1"
                VerticalAlignment="Center"
                Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.6" />

            <Button Content="Open Folder" Grid.Column="2"
                Tag="{Binding Name}" Click="OpenFolder_Click"
                Padding="8,4" />

            <Button Content="Delete" Grid.Column="3"
                Tag="{Binding Name}" Click="DeleteCase_Click"
                Padding="8,4" />
          </Grid>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>

    <TextBlock x:Name="NoCasesText" Grid.Row="2"
        Text="No cases found. Create one from a template above."
        HorizontalAlignment="Center" VerticalAlignment="Center"
        Opacity="0.5" Visibility="Collapsed" />
  </Grid>
</Page>
