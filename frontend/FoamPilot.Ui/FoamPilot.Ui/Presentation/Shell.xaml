<UserControl x:Class="FoamPilot.Ui.Presentation.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FoamPilot.Ui.Presentation"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls">

  <Grid Background="{ThemeResource BackgroundBrush}">
    <muxc:NavigationView x:Name="NavView"
        IsBackButtonVisible="Collapsed"
        IsPaneToggleButtonVisible="True"
        IsSettingsVisible="False"
        PaneDisplayMode="Left"
        SelectionChanged="NavView_SelectionChanged"
        OpenPaneLength="240">

      <muxc:NavigationView.MenuItems>
        <!-- Primary: the guided journey -->
        <muxc:NavigationViewItem Content="New Simulation" Tag="Wizard" Icon="Add" />
        <muxc:NavigationViewItem Content="My Simulations" Tag="MySimulations" Icon="Library" />

        <muxc:NavigationViewItemSeparator />

        <!-- Advanced: power-user tools -->
        <muxc:NavigationViewItemHeader Content="Advanced" />
        <muxc:NavigationViewItem Content="Dashboard" Tag="Dashboard" Icon="Home" />
        <muxc:NavigationViewItem Content="Case Browser" Tag="Cases" Icon="AllApps" />
        <muxc:NavigationViewItem Content="Run Control" Tag="RunControl" Icon="Play" />
        <muxc:NavigationViewItem Content="Logs" Tag="Logs" Icon="List" />
        <muxc:NavigationViewItem Content="Dict Editor" Tag="DictEditor" Icon="Edit" />
      </muxc:NavigationView.MenuItems>

      <muxc:NavigationView.FooterMenuItems>
        <muxc:NavigationViewItem Content="Settings" Tag="Settings" Icon="Setting" />
      </muxc:NavigationView.FooterMenuItems>

      <!-- Content area where pages render -->
      <ContentControl x:Name="NavContent"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch"
          HorizontalContentAlignment="Stretch"
          VerticalContentAlignment="Stretch" />
    </muxc:NavigationView>
  </Grid>
</UserControl>
